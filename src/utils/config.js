// Email address to contact for support
const supportEmailAddress = "fbabears@gmail.com";

// URL of starting guide
const guidaPerIniziare = "http://google.com";

const config = {
  supportEmailAddress,
  guidaPerIniziare,
};

export default config;
